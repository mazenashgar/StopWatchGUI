# Stopwatch GUI
A stopwatch that counts upwards, using milliseconds, seconds, and minutes. The stopwatch can be saved to a file and loaded from the file. The program checks if the parameters passed to any method is valid, if not, the program will throw an exception.
